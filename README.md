# be-golang-fiber

```
migrate -path db/migrations -database "mysql://root@tcp(localhost:3306)/fiber" -verbose up

migrate -path db/migrations -database "mysql://root@tcp(localhost:3306)/fiber" -verbose down
```